// import express from 'express';
// import {
//   publishNewsletter,
//   sendPublishedNewsletter
// } from '../controllers/publishedNewsletter.controller.js';
// import { authenticate } from '../middleware/auth.middleware.js';

// const router = express.Router();

// // Protect these routes with authentication
// router.post('/:id/publish', authenticate, publishNewsletter);
// router.post('/:id/send', authenticate, sendPublishedNewsletter);

// export default router;